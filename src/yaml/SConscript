# SConscript
# vim: set ft=python:
# Eryn Wells <eryn@erynwells.me>

Import('env')


files = [
    'parsers.cc',
    'objectParser.cc',
    'scalarMappingParser.cc',
    'sceneParser.cc',
]

objs = []
for f in files:
    objs.append(env.Object(f))

Return('objs')
