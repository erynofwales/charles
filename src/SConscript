# SConscript
# vim: set ft=python:
# Eryn Wells <eryn@erynwells.me>

Import('env')

files = Split("""
    basics.c
    charles.c
    scene.c
""")

prog = env.Program('charles', files)
env.Alias('charles', prog)
