# SConscript
# vim: set ft=python:
# Eryn Wells <eryn@erynwells.me>

Import('env')

files = Split("""
    basics.c
    camera.c
    charles.c
    object.c
    scene.c
    texture.c
    writer_png.c
""")

prog = env.Program('charles', files)
env.Alias('charles', prog)
